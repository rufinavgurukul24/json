#* json. load(): json. load() accepts file object,
    #  parses the JSON data, populates a Python dictionary 
    # with the data and returns it back to you. Syntax: json.load(file object) Example: Suppose the JSON file looks like this: ...
    # *json. loads(): If you have a JSON string, 
    # you can parse it by using the json. loads() method. json.



# Notes :-

# String >=  json.loads() >= json objects

# Json object >= json.dumps() >= string

# Python object>= json.dump() >= json object'

# Json object >= json.load() >= python object
